# Summary of changes for run 65f37f4d-cc67-4763-9444-68003081b3a2
WO-9 (the "harvest" commission) is delivered: the base artifact from the tarball is unpacked into the project, `RequestProject/PlufWO9.lean` is complete, and the whole project builds green (`lake build` — all prior work-order files untouched and still compiling, `PlufWO7a.lean` neither edited nor deleted).

**Every contracted item is proved, including all eight `True`-valued placeholders, whose real statements I supplied.** No `sorry`, `admit`, `axiom` or `native_decide`; every `#print axioms` line reports exactly `[propext, Classical.choice, Quot.sound]`; CH appears only as a hypothesis.

What was delivered, by part:
- **A (Proposition 2.3).** A1 with the unused oddness hypothesis dropped, plus a more general vector-space form that needs no inner product or topology; A2 for the paper's separable space. Packaging chosen: two named corollaries rather than one disjunction, since the two regimes have different primeness clauses. The *optional* item turned out cheap and is proved: the WO-6 triple argument runs at an arbitrary infinite orthonormal basis, so Proposition 2.3 holds in every real Hilbert space with an infinite orthonormal basis, separable or not.
- **B (essential spectrum).** `essSpec` re-sited as the stable name (redefined, not re-exported); closedness; B3. B2's statement supplied with the minimal hypotheses actually needed — V closed with finite-dimensional orthocomplement, and nothing else: no self-adjointness, no finite-rank/Calkin/Fredholm input — and with a strengthened conclusion (an orthonormal, hence weakly null, sequence inside V). An operator form, `essSpec T ⊆ essSpec (compress T V)`, is added.
- **C (substitute for spectral subspaces).** C1 now takes only a point of the essential spectrum and a caller-chosen decay rate; a version confining the vectors to a closed finite-codimension subspace is provided, and B2/C1 are both instances of a single engine. C2's tolerance clause is the homogeneous bound `‖T x − lam·x‖ ≤ (∑' n, ε n)·‖x‖` on the whole closed span, together with infinite-dimensionality; the reasons for that shape and the rejected alternatives are recorded in the report.
- **D (general Hilbert-basis blocks).** `blockB` with both membership characterizations and closedness; D2 delivered as a definitional *equality* with the WO-1 standard-basis block (no bridging isometry needed); D3 universe-polymorphic; D4 (the corrected Q2) with the explicit warning that it must not be generalized.
- **E (ω₁).** E1 with the universe lifting hidden from callers, and E2 as a combinator with one step function and one bookkeeping map and no successor/limit split.

`REPORT-WO9.md` is the report; its first section is the signature table, giving each contracted item's final statement as elaborated (plus the supporting API), followed by the census, the report-rather-than-repair items (notably A2's contract hypothesis `hcl`, which is retained but demonstrably unnecessary), the axiom audit, and notes for WO-10 onward.